import { MapBattlePipe } from './map-battle.pipe';

describe('MapBattlePipe', () => {
  it('create an instance', () => {
    const pipe = new MapBattlePipe();
    expect(pipe).toBeTruthy();
  });
});
