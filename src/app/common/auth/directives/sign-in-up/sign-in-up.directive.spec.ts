import { SignInUpDirective } from './sign-in-up.directive';

describe('SignInUpDirective', () => {
  it('should create an instance', () => {
    const directive = new SignInUpDirective();
    expect(directive).toBeTruthy();
  });
});
