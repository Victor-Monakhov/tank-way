import { InputHeightPipe } from './input-height.pipe';

describe('InputHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new InputHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
