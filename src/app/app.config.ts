export enum NAVIGATE {
  HOME = '',
  DEMO = 'demo'
}

export enum WIN_SIZES {
  XS = 0,
  SM = 576,
  MD = 768,
  LG = 992,
  XL = 1200,
}

export enum CONFIG {
  SERVER_HOST = 'http://localhost:8080',
}

